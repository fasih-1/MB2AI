[pytest]
testpaths = tests
pythonpath = .
addopts = -q
filterwarnings =
    # Dependency deprecations we do not control; keep our own warnings visible.
    ignore:Please use `import python_multipart` instead.:PendingDeprecationWarning
    ignore:PyPDF2 is deprecated.*:DeprecationWarning
